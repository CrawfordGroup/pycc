name: CI

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"
  schedule:
    - cron: "0 0 * * *"

jobs:
  test:
    defaults:
      run:
        shell: bash -l {0}

    name: PyCC Test ${{ matrix.os }}, Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [macOS-latest, ubuntu-latest]
        python-version: ['3.11']

    steps:
    - uses: actions/checkout@v3

    - name: Additional info about the build
      shell: bash
      run: |
        uname -a
        df -h
        ulimit -a

    - name: Create Psi4 Environment
      uses: conda-incubator/setup-miniconda@v3
      with:
        miniconda-version: "latest"
        activate-environment: p4env
        environment-file: devtools/conda-envs/psi.yaml
        python-version: ${{ matrix.python-version }}
        auto-activate-base: false
        add-pip-as-python-dependency: true

    - name: Install Psi4
      run: |
        conda info
        conda list --show-channel-urls
        hash -r
        env
        conda install psi4 python=${{ matrix.python-version }} numpy=2 -c conda-forge
        which conda python psi4
        ls -l $CONDA

    - name: Buildtime Conda environment
      run: |
        conda info
        conda list

    - name: Test Psi4 Python Loading
      run: |
        export PATH=$CONDA_PREFIX/bin:$PATH
        export PYTHONPATH=$CONDA_PREFIX/lib:$PYTHONPATH
        python -c 'import psi4;print(psi4.__file__, psi4.__version__)' || true
        which conda python psi4

    - name: Install PyCC and Deps
      run: |
        pip install -e .
        pip install pytest
        conda list
        python -c 'import psi4;print(psi4.__file__, psi4.__version__)' || true
        which conda python psi4

    - name: Test and generate coverage report
      run: |
        export KMP_DUPLICATE_LIB_OK=TRUE
        pip install coverage
        python -c 'import psi4;print(psi4.__file__, psi4.__version__)' || true
        which conda python psi4
        coverage run -m pytest
        coverage xml
        ls -l

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
